dplyr::filter(dtype=='pre_cw', sample %in% t_win) %>%
mutate(time = sample / fs, bin = cut(frequency, breaks, labels)) %>%
mutate(time = time - min(time),
bin = factor(bin, levels = c('Beta (1,4]', 'Alpha (4,8]', 'Theta (8,12]', 'Delta (12,30]', 'Gamma (30,40]'))) %>%
group_by(bin, time) %>%
summarize(power = mean(power)) %>%
ggplot(aes(x = time, y = bin, fill = power)) +
#geom_raster(interpolate=TRUE) +
geom_tile() +
#scale_y_log10() +
scale_fill_viridis_c(option = 'plasma', limits = c(0, max_p), breaks = seq(0, max_p, length.out=2)) +
labs(
x = 'Time (s)',
y = 'Frequency band (Hz)',
fill = 'Power'
) +
theme_bw() +
theme(axis.ticks = element_blank(),
panel.grid = element_blank(),
text = element_text(size = 16),
axis.text = element_text(size = axis_text),
legend.position = 'none')
p4a <- waves %>%
dplyr::filter(dtype=='post_cw', sample %in% t_win) %>%
mutate(time = sample / fs, bin = cut(frequency, breaks, labels)) %>%
mutate(time = time - min(time),
bin = factor(bin, levels = c('Beta (1,4]', 'Alpha (4,8]', 'Theta (8,12]', 'Delta (12,30]', 'Gamma (30,40]'))) %>%
group_by(bin, time) %>%
summarize(power = mean(power)) %>%
ggplot(aes(x = time, y = bin, fill = power)) +
#geom_raster(interpolate=TRUE) +
geom_tile() +
#scale_y_log10() +
scale_fill_viridis_c(option = 'plasma', limits = c(0, max_p), breaks = seq(0, max_p, length.out=2)) +
labs(
x = 'Time (s)',
y = 'Frequency band (Hz)',
fill = 'Power'
) +
theme_bw() +
theme(axis.ticks = element_blank(),
panel.grid = element_blank(),
text = element_text(size = 16),
axis.text = element_text(size = axis_text),
legend.position = 'none')
g <- ggarrange(ggarrange(p1, p2a, nrow = 2), ggarrange(p3, p4a, nrow = 2),
nrow = 2, labels = c('A.', 'B.'))
g
labels
breaks <- c(0, 1, 4, 8, 12, 30, 40)
labels <- c('init', 'Delta', 'Theta', 'Alpha', 'Beta', 'Gamma')
bins <- unique(cut(tf$frequency, breaks=breaks))
labels <- paste(labels, bins, sep=' ')
labels
bins
bins <- rev(unique(cut(tf$frequency, breaks=breaks)))
bins
bins <- rev(unique(cut(0:40, breaks=breaks)))
bins
bins <- unique(cut(0:40, breaks=breaks))
bins
bins <- unique(cut(1:40, breaks=breaks))
bins
labels <- paste(labels, bins, sep=' ')
labels
breaks <- c(0, 1, 4, 8, 12, 30, 40)
labels <- c('init', 'Delta', 'Theta', 'Alpha', 'Beta', 'Gamma')
bins <- unique(cut(1:40, breaks=breaks))
labels <- paste(labels, bins, sep=' ')
labels
#alpha <- tf %>%
tf %>%
mutate(bin = cut(frequency, breaks, labels)) %>%
head()
#alpha <- tf %>%
tf %>%
mutate(bin = cut(frequency, breaks, labels)) %>%
summarize(unique(frequency), unique(bin))
#alpha <- tf %>%
tf %>%
mutate(bin = cut(frequency, breaks, labels)) %>%
View()
#alpha <- tf %>%
tf %>%
mutate(bin = cut(frequency, breaks, labels)) %>%
tail()
labels
breaks
#alpha <- tf %>%
tf %>%
mutate(bin = cut(frequency, breaks, labels)) %>%
group_by(frequency) %>%
summarize(unique(bin))
#alpha <- tf %>%
tf %>%
mutate(bin = cut(frequency, breaks, labels)) %>%
group_by(frequency) %>%
summarize(unique(bin)) %>%
View()
alpha <- tf %>%
mutate(bin = cut(frequency, breaks, labels)) %>%
summarize(unique(frequency), unique(bin))
alpha <- tf %>%
mutate(bin = cut(frequency, breaks, labels)) %>%
mutate(is_alpha = ifelse(bin == 'Alpha (4,8]', 'alpha', 'non_alpha')) %>%
group_by(sample, is_alpha) %>%
summarize(power = sum(power)) %>%
spread(is_alpha, power) %>%
mutate(alpha_adv = alpha / (alpha + non_alpha)) %>%
select(sample, alpha_adv)
#alpha <- tf %>%
tf %>%
mutate(bin = cut(frequency, breaks, labels)) %>%
mutate(is_alpha = ifelse(bin == 'Alpha (4,8]', 'alpha', 'non_alpha')) %>%
group_by(sample, is_alpha) %>%
summarize(power = sum(power)) %>%
spread(is_alpha, power) %>%
head()
#alpha <- tf %>%
tf %>%
mutate(bin = cut(frequency, breaks, labels)) %>%
mutate(is_alpha = ifelse(bin == 'Alpha (8,12]', 'alpha', 'non_alpha')) %>%
group_by(sample, is_alpha) %>%
summarize(power = sum(power)) %>%
spread(is_alpha, power) %>%
head()
#alpha <- tf %>%
tf %>%
mutate(bin = cut(frequency, breaks, labels)) %>%
mutate(is_alpha = ifelse(bin == 'Alpha (8,12]', 'alpha', 'non_alpha')) %>%
group_by(sample, is_alpha) %>%
summarize(power = sum(power)) %>%
spread(is_alpha, power) %>%
mutate(alpha_adv = alpha / (alpha + non_alpha)) %>%
select(sample, alpha_adv) %>%
head()
alpha <- tf %>%
mutate(bin = cut(frequency, breaks, labels)) %>%
mutate(is_alpha = ifelse(bin == 'Alpha (8,12]', 'alpha', 'non_alpha')) %>%
group_by(sample, is_alpha) %>%
summarize(power = sum(power)) %>%
spread(is_alpha, power) %>%
mutate(alpha_adv = alpha / (alpha + non_alpha)) %>%
select(sample, alpha_adv)
ma <- rollmedian(alpha$alpha_adv, window_size * fs, fill = NA)
plot(ma)
ma <- rollmedian(alpha$alpha_adv, window_size * 10, fill = NA)
plot(ma)
window_size
ma <- rollmedian(alpha$alpha_adv, window_size * fs, fill = NA)
plot(ma)
ma <- data.frame(sample = 1:(length(ma)), alpha = ma)
best_sample <- ma %>%
dplyr::filter(sample >= 10000, sample < 100000) %>%
#dplyr::filter(sample >= 10000 & sample <= 150000) %>%
dplyr::filter(alpha == max(alpha)) %>%
print() %>%
pull(sample)
best_sample <- round(median(best_sample))
start <- best_sample - (window_size/2 * fs)
t_win <- start:(start + window_size*fs)
# --- FIGURE: INTERLEAVED TIME DOMAIN - TIME-FREQ DOMAIN ---  #
p1 <- d %>%
dplyr::filter(dtype == 'pre_cw', channel == 'O2', sample %in% t_win) %>%
mutate(time = sample / fs) %>%
mutate(time = time - min(time)) %>%
ggplot(aes(x = time, y = voltage)) +
geom_line() +
labs(
x = 'Time (s)',
y = 'EEG potential (V)',
title = 'Before CW regression (after MR gradient correction)'
) +
ylim(-8e-05, 7e-5) +
theme_bw() +
theme(axis.ticks = element_blank(),
panel.grid = element_blank(),
text = element_text(size = 16),
axis.text = element_text(size = axis_text))
max_p <- ceiling(max(waves[waves$sample %in% t_win & waves$dtype=='post_cw',]$power))
p2 <- waves %>%
dplyr::filter(dtype=='pre_cw', sample %in% t_win) %>%
mutate(time = sample / fs) %>%
mutate(time = time - min(time)) %>%
ggplot(aes(x = time, y = frequency, fill = power)) +
geom_raster(interpolate=TRUE) +
scale_y_log10() +
scale_fill_viridis_c(option = 'plasma', limits = c(0, max_p), breaks = seq(0, max_p, length.out=2)) +
labs(
x = 'Time (s)',
y = 'Frequency (Hz)',
fill = 'Power'
) +
theme_bw() +
theme(axis.ticks = element_blank(),
panel.grid = element_blank(),
text = element_text(size = 16),
axis.text = element_text(size = axis_text),
legend.position = 'none')
p3 <- d %>%
dplyr::filter(dtype == 'post_cw', channel == 'O2', sample %in% t_win) %>%
mutate(time = sample / fs) %>%
mutate(time = time - min(time)) %>%
ggplot(aes(x = time, y = voltage)) +
geom_line() +
labs(
x = 'Time (s)',
y = 'EEG potential (V)',
title = 'After CW regression'
) +
ylim(-8e-05, 7e-5) +
theme_bw() +
theme(axis.ticks = element_blank(),
panel.grid = element_blank(),
text = element_text(size = 16),
axis.text = element_text(size = axis_text))
p4 <- waves %>%
dplyr::filter(dtype=='post_cw', sample %in% t_win) %>%
mutate(time = sample / fs) %>%
mutate(time = time - min(time)) %>%
ggplot(aes(x = time, y = frequency, fill = power)) +
geom_raster(interpolate=TRUE) +
scale_y_log10() +
scale_fill_viridis_c(option = 'plasma', limits = c(0, max_p), breaks = seq(0, max_p, length.out=2)) +
labs(
x = 'Time (s)',
y = 'Frequency (Hz)',
fill = 'Power',
caption = glue::glue('Max power: {max_p}')
) +
theme_bw() +
theme(axis.ticks = element_blank(),
panel.grid = element_blank(),
text = element_text(size = 16),
axis.text = element_text(size = axis_text),
legend.position = 'none')
# --- BY FREQ BAND --- #
# Bin frequencies
breaks <- c(0, 1, 4, 8, 12, 30, 40)
labels <- c('init', 'Delta', 'Theta', 'Alpha', 'Beta', 'Gamma')
bins <- unique(cut(waves$frequency, breaks=breaks))
labels <- paste(labels, bins, sep=' ')
p2a <- waves %>%
dplyr::filter(dtype=='pre_cw', sample %in% t_win) %>%
mutate(time = sample / fs, bin = cut(frequency, breaks, labels)) %>%
mutate(time = time - min(time),
bin = factor(bin, levels = c('Beta (1,4]', 'Alpha (4,8]', 'Theta (8,12]', 'Delta (12,30]', 'Gamma (30,40]'))) %>%
group_by(bin, time) %>%
summarize(power = mean(power)) %>%
ggplot(aes(x = time, y = bin, fill = power)) +
#geom_raster(interpolate=TRUE) +
geom_tile() +
#scale_y_log10() +
scale_fill_viridis_c(option = 'plasma', limits = c(0, max_p), breaks = seq(0, max_p, length.out=2)) +
labs(
x = 'Time (s)',
y = 'Frequency band (Hz)',
fill = 'Power'
) +
theme_bw() +
theme(axis.ticks = element_blank(),
panel.grid = element_blank(),
text = element_text(size = 16),
axis.text = element_text(size = axis_text),
legend.position = 'none')
p4a <- waves %>%
dplyr::filter(dtype=='post_cw', sample %in% t_win) %>%
mutate(time = sample / fs, bin = cut(frequency, breaks, labels)) %>%
mutate(time = time - min(time),
bin = factor(bin, levels = c('Beta (1,4]', 'Alpha (4,8]', 'Theta (8,12]', 'Delta (12,30]', 'Gamma (30,40]'))) %>%
group_by(bin, time) %>%
summarize(power = mean(power)) %>%
ggplot(aes(x = time, y = bin, fill = power)) +
#geom_raster(interpolate=TRUE) +
geom_tile() +
#scale_y_log10() +
scale_fill_viridis_c(option = 'plasma', limits = c(0, max_p), breaks = seq(0, max_p, length.out=2)) +
labs(
x = 'Time (s)',
y = 'Frequency band (Hz)',
fill = 'Power'
) +
theme_bw() +
theme(axis.ticks = element_blank(),
panel.grid = element_blank(),
text = element_text(size = 16),
axis.text = element_text(size = axis_text),
legend.position = 'none')
g <- ggarrange(ggarrange(p1, p2a, nrow = 2), ggarrange(p3, p4a, nrow = 2),
nrow = 2, labels = c('A.', 'B.'))
g
breaks <- c(0, 1, 4, 8, 12, 30, 40)
labels <- c('init', 'Delta', 'Theta', 'Alpha', 'Beta', 'Gamma')
bins <- unique(cut(waves$frequency, breaks=breaks))
labels <- paste(labels, bins, sep=' ')
labels
bins
bins <- unique(cut(1:40, breaks=breaks))
breaks <- c(0, 1, 4, 8, 12, 30, 40)
labels <- c('init', 'Delta', 'Theta', 'Alpha', 'Beta', 'Gamma')
bins <- unique(cut(1:40, breaks=breaks))
labels <- paste(labels, bins, sep=' ')
labels
p2a <- waves %>%
dplyr::filter(dtype=='pre_cw', sample %in% t_win) %>%
mutate(time = sample / fs, bin = cut(frequency, breaks, labels)) %>%
mutate(time = time - min(time),
bin = factor(bin, levels = c('Beta (1,4]', 'Alpha (4,8]', 'Theta (8,12]', 'Delta (12,30]', 'Gamma (30,40]'))) %>%
group_by(bin, time) %>%
summarize(power = mean(power)) %>%
ggplot(aes(x = time, y = bin, fill = power)) +
#geom_raster(interpolate=TRUE) +
geom_tile() +
#scale_y_log10() +
scale_fill_viridis_c(option = 'plasma', limits = c(0, max_p), breaks = seq(0, max_p, length.out=2)) +
labs(
x = 'Time (s)',
y = 'Frequency band (Hz)',
fill = 'Power'
) +
theme_bw() +
theme(axis.ticks = element_blank(),
panel.grid = element_blank(),
text = element_text(size = 16),
axis.text = element_text(size = axis_text),
legend.position = 'none')
p4a <- waves %>%
dplyr::filter(dtype=='post_cw', sample %in% t_win) %>%
mutate(time = sample / fs, bin = cut(frequency, breaks, labels)) %>%
mutate(time = time - min(time),
bin = factor(bin, levels = c('Beta (1,4]', 'Alpha (4,8]', 'Theta (8,12]', 'Delta (12,30]', 'Gamma (30,40]'))) %>%
group_by(bin, time) %>%
summarize(power = mean(power)) %>%
ggplot(aes(x = time, y = bin, fill = power)) +
#geom_raster(interpolate=TRUE) +
geom_tile() +
#scale_y_log10() +
scale_fill_viridis_c(option = 'plasma', limits = c(0, max_p), breaks = seq(0, max_p, length.out=2)) +
labs(
x = 'Time (s)',
y = 'Frequency band (Hz)',
fill = 'Power'
) +
theme_bw() +
theme(axis.ticks = element_blank(),
panel.grid = element_blank(),
text = element_text(size = 16),
axis.text = element_text(size = axis_text),
legend.position = 'none')
g <- ggarrange(ggarrange(p1, p2a, nrow = 2), ggarrange(p3, p4a, nrow = 2),
nrow = 2, labels = c('A.', 'B.'))
g
g
breaks
labelks
labels
#p2a <- waves %>%
waves %>%
dplyr::filter(dtype=='pre_cw', sample %in% t_win) %>%
mutate(time = sample / fs, bin = cut(frequency, breaks, labels)) %>%
head()
#p2a <- waves %>%
waves %>%
dplyr::filter(dtype=='pre_cw', sample %in% t_win) %>%
mutate(time = sample / fs, bin = cut(frequency, breaks, labels)) %>%
tail()
p2a <- waves %>%
dplyr::filter(dtype=='pre_cw', sample %in% t_win) %>%
mutate(time = sample / fs, bin = cut(frequency, breaks, labels)) %>%
mutate(time = time - min(time),
bin = factor(bin, levels = c('Delta (1,4]', 'Theta (4,8]', 'Alpha (8,12]', 'Beta (12,30]', 'Gamma (30,40]'))) %>%
group_by(bin, time) %>%
summarize(power = mean(power)) %>%
ggplot(aes(x = time, y = bin, fill = power)) +
#geom_raster(interpolate=TRUE) +
geom_tile() +
#scale_y_log10() +
scale_fill_viridis_c(option = 'plasma', limits = c(0, max_p), breaks = seq(0, max_p, length.out=2)) +
labs(
x = 'Time (s)',
y = 'Frequency band (Hz)',
fill = 'Power'
) +
theme_bw() +
theme(axis.ticks = element_blank(),
panel.grid = element_blank(),
text = element_text(size = 16),
axis.text = element_text(size = axis_text),
legend.position = 'none')
p4a <- waves %>%
dplyr::filter(dtype=='post_cw', sample %in% t_win) %>%
mutate(time = sample / fs, bin = cut(frequency, breaks, labels)) %>%
mutate(time = time - min(time),
bin = factor(bin, levels = c('Delta (1,4]', 'Theta (4,8]', 'Alpha (8,12]', 'Beta (12,30]', 'Gamma (30,40]'))) %>%
group_by(bin, time) %>%
summarize(power = mean(power)) %>%
ggplot(aes(x = time, y = bin, fill = power)) +
#geom_raster(interpolate=TRUE) +
geom_tile() +
#scale_y_log10() +
scale_fill_viridis_c(option = 'plasma', limits = c(0, max_p), breaks = seq(0, max_p, length.out=2)) +
labs(
x = 'Time (s)',
y = 'Frequency band (Hz)',
fill = 'Power'
) +
theme_bw() +
theme(axis.ticks = element_blank(),
panel.grid = element_blank(),
text = element_text(size = 16),
axis.text = element_text(size = axis_text),
legend.position = 'none')
g <- ggarrange(ggarrange(p1, p2a, nrow = 2), ggarrange(p3, p4a, nrow = 2),
nrow = 2, labels = c('A.', 'B.'))
g
g
ggsave(filename='r01_figure_a.png', plot = g, height = 14, width = 10, units = 'in', dpi = 300)
p2a <- waves %>%
dplyr::filter(dtype=='pre_cw', sample %in% t_win) %>%
mutate(time = sample / fs, bin = cut(frequency, breaks, labels)) %>%
mutate(time = time - min(time),
bin = factor(bin, levels = c('Delta (1,4]', 'Theta (4,8]', 'Alpha (8,12]', 'Beta (12,30]', 'Gamma (30,40]'))) %>%
group_by(bin, time) %>%
summarize(power = mean(power)) %>%
ggplot(aes(x = time, y = bin, fill = power)) +
geom_raster(interpolate=TRUE) +
#scale_y_log10() +
scale_fill_viridis_c(option = 'plasma', limits = c(0, max_p), breaks = seq(0, max_p, length.out=2)) +
labs(
x = 'Time (s)',
y = 'Frequency band (Hz)',
fill = 'Power'
) +
theme_bw() +
theme(axis.ticks = element_blank(),
panel.grid = element_blank(),
text = element_text(size = 16),
axis.text = element_text(size = axis_text),
legend.position = 'none')
p4a <- waves %>%
dplyr::filter(dtype=='post_cw', sample %in% t_win) %>%
mutate(time = sample / fs, bin = cut(frequency, breaks, labels)) %>%
mutate(time = time - min(time),
bin = factor(bin, levels = c('Delta (1,4]', 'Theta (4,8]', 'Alpha (8,12]', 'Beta (12,30]', 'Gamma (30,40]'))) %>%
group_by(bin, time) %>%
summarize(power = mean(power)) %>%
ggplot(aes(x = time, y = bin, fill = power)) +
geom_raster(interpolate=TRUE) +
#scale_y_log10() +
scale_fill_viridis_c(option = 'plasma', limits = c(0, max_p), breaks = seq(0, max_p, length.out=2)) +
labs(
x = 'Time (s)',
y = 'Frequency band (Hz)',
fill = 'Power'
) +
theme_bw() +
theme(axis.ticks = element_blank(),
panel.grid = element_blank(),
text = element_text(size = 16),
axis.text = element_text(size = axis_text),
legend.position = 'none')
g <- ggarrange(ggarrange(p1, p2a, nrow = 2), ggarrange(p3, p4a, nrow = 2),
nrow = 2, labels = c('A.', 'B.'))
ggsave(filename='r01_figure_a.png', plot = g, height = 14, width = 10, units = 'in', dpi = 300)
p2a <- waves %>%
dplyr::filter(dtype=='pre_cw', sample %in% t_win) %>%
mutate(time = sample / fs, bin = cut(frequency, breaks, labels)) %>%
mutate(time = time - min(time),
bin = factor(bin, levels = c('Delta (1,4]', 'Theta (4,8]', 'Alpha (8,12]', 'Beta (12,30]', 'Gamma (30,40]'))) %>%
group_by(bin, time) %>%
summarize(power = mean(power)) %>%
ggplot(aes(x = time, y = bin, fill = power)) +
geom_raster(interpolate=FALSE) +
#scale_y_log10() +
scale_fill_viridis_c(option = 'plasma', limits = c(0, max_p), breaks = seq(0, max_p, length.out=2)) +
labs(
x = 'Time (s)',
y = 'Frequency band (Hz)',
fill = 'Power'
) +
theme_bw() +
theme(axis.ticks = element_blank(),
panel.grid = element_blank(),
text = element_text(size = 16),
axis.text = element_text(size = axis_text),
legend.position = 'none')
p4a <- waves %>%
dplyr::filter(dtype=='post_cw', sample %in% t_win) %>%
mutate(time = sample / fs, bin = cut(frequency, breaks, labels)) %>%
mutate(time = time - min(time),
bin = factor(bin, levels = c('Delta (1,4]', 'Theta (4,8]', 'Alpha (8,12]', 'Beta (12,30]', 'Gamma (30,40]'))) %>%
group_by(bin, time) %>%
summarize(power = mean(power)) %>%
ggplot(aes(x = time, y = bin, fill = power)) +
geom_raster(interpolate=FALSE) +
#scale_y_log10() +
scale_fill_viridis_c(option = 'plasma', limits = c(0, max_p), breaks = seq(0, max_p, length.out=2)) +
labs(
x = 'Time (s)',
y = 'Frequency band (Hz)',
fill = 'Power'
) +
theme_bw() +
theme(axis.ticks = element_blank(),
panel.grid = element_blank(),
text = element_text(size = 16),
axis.text = element_text(size = axis_text),
legend.position = 'none')
g <- ggarrange(ggarrange(p1, p2a, nrow = 2), ggarrange(p3, p4a, nrow = 2),
nrow = 2, labels = c('A.', 'B.'))
ggsave(filename='r01_figure_a.png', plot = g, height = 14, width = 10, units = 'in', dpi = 300)
q()
